ifndef::imagesdir[:imagesdir: ../images]

[[section-runtime-view]]
== Runtime View

ifdef::arc42help[]
[role="arc42help"]
****
.Contents
The runtime view describes concrete behavior and interactions of the system’s building blocks in form of scenarios from the following areas:

* important use cases or features: how do building blocks execute them?
* interactions at critical external interfaces: how do building blocks cooperate with users and neighboring systems?
* operation and administration: launch, start-up, stop
* error and exception scenarios

Remark: The main criterion for the choice of possible scenarios (sequences, workflows) is their *architectural relevance*. It is *not* important to describe a large number of scenarios. You should rather document a representative selection.

.Motivation
You should understand how (instances of) building blocks of your system perform their job and communicate at runtime.
You will mainly capture scenarios in your documentation to communicate your architecture to stakeholders that are less willing or able to read and understand the static models (building block view, deployment view).

.Form
There are many notations for describing scenarios, e.g.

* numbered list of steps (in natural language)
* activity diagrams or flow charts
* sequence diagrams
* BPMN or EPCs (event process chains)
* state machines
* ...


.Further Information

See https://docs.arc42.org/section-6/[Runtime View] in the arc42 documentation.

****
endif::arc42help[]

The scenarios below cover the architecturally relevant flows of *Drop*:
the use cases that touch several building blocks, cross trust boundaries
or exercise the resilience and security concepts described in
chapter 8.

=== Scenario 1 – Search and Book a Ride

Trigger:: A passenger searches for a ride and books one of the
results.

Flow::
. *Flutter Client* (see <<section-building-block-view>>) requests a
  ride by sending pickup location, destination and desired time to
  the *Backend Monolith*.
. *Matching* (see <<section-building-block-view>>, Level 3 – Rides)
  queries the *Map & Routing Provider* (see
  <<section-context-and-scope>>) for distance and ETA and returns a
  list of nearby available drivers.
. The passenger selects a ride; *Rides* creates a `Ride` in state
  `REQUESTED` and publishes a `RideRequested` event on the *Event
  Bus* (Apache Kafka).
. *Notifications* pushes the request to the selected driver via
  Firebase Cloud Messaging.

Notable aspects::
* All calls to the Map & Routing Provider go through a *Circuit
  Breaker* (see <<section-concepts>>); on open circuit the request
  fails fast with an RFC 7807 error.
* The booking call is idempotent (the client sends an *Idempotency
  Key*, see <<section-concepts>>) so a retry on a flaky network does
  not create duplicate rides (covered by quality scenario QS-2 in
  <<section-quality-scenarios>>).


=== Scenario 2 – Ride Completion and Payment

Trigger:: The driver marks the ride as completed.

Flow::
. *Rides* (see <<section-building-block-view>>, Level 3 – Rides)
  transitions the `Ride` to `COMPLETED` and publishes `RideCompleted`
  on the *Event Bus*.
. The *Payments* façade inside the monolith forwards the request to
  the extracted *Payment Service* (see <<section-building-block-view>>,
  Level 1), which calls *Stripe Connect* (see
  <<section-context-and-scope>>) to charge the passenger, pay out
  the driver and retain the commission.
. Stripe confirms via webhook; the *Payment Service* publishes
  `PaymentSettled` on the *Event Bus*.
. *Notifications* sends a receipt to the passenger and a payout
  notice to the driver.
. *Analytics* (see <<section-building-block-view>>, Level 3 –
  Analytics) consumes `RideCompleted` and `PaymentSettled` and
  stores the resulting metrics through its `AnalyticsRepository`.

Notable aspects::
* No card data ever touches Drop – only Stripe tokens are stored
  (see *Security and Authorisation* in <<section-concepts>>, ADR-3).
* Payment calls are idempotent; on network failure they are retried
  with *Retry with Exponential Backoff* (see <<section-concepts>>).
* If Stripe is unavailable, the ride is still marked completed and
  the charge is queued for later settlement (covered by quality
  scenario QS-3 in <<section-quality-scenarios>>).